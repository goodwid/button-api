/*
 *  Populate this file, save it, and run 'npm run projectInit'.
 *  the url should support
 */

var port = 9000
var localIp = '192.168.1.100'

module.exports = {
   ssid: 'SSID here',
   wifiPw: 'wifi PW here',
   url: `http://${localIp}:${port}/button/``
 }
